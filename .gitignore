# DB artifacts (regenerated by bin/sync-from-bluehost.sh)
*.db
*.db-journal
*.db.tmp
# cloud-managed recovery state, committed by .github/workflows/hunter.yml
!recovery/hunter.db
powerpage*.sql
powerpage*.sql.gz
*.sql.gz
backups/
logs/

# Tooling fetched at runtime
mysql2sqlite

# Python virtualenv (for the Wayback scraper)
.venv/

# Node/Vite dev workspace artifacts
pp-twin-dev/node_modules/
pp-twin-dev/dist/
pp-twin-dev/public/powerpage.db

# Editor / OS / Claude session
.DS_Store
.claude/
.idea/
.vscode/
.wrangler/

# Local secrets (host, user, key paths for bin/sync-from-bluehost.sh)
.env
